const express=require('express');const router=express.Router();const Course=require('../models/Course');router.get('/',async(req,res)=>{const page=Math.max(0,parseInt(req.query.page)||0);const limit=Math.min(50,parseInt(req.query.limit)||12);const skip=page*limit;const [items,total]=await Promise.all([Course.find({}).skip(skip).limit(limit).lean(),Course.countDocuments({})]);res.json({ok:true,page,limit,total,items});});module.exports=router;