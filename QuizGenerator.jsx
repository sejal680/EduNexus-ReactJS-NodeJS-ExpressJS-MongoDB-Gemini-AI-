import React,{useState}from'react';import api from'../services/api';export default function QuizGenerator(){const[text,setText]=useState('');const[result,setResult]=useState(null);const[loading,setLoading]=useState(false);const onGenerate=async()=>{setLoading(true);const r=await api.post('/gemini/generate-quiz',{text});setResult(r.data.payload);setLoading(false);};return(<div><h3>AI Quiz Generator</h3><textarea value={text}onChange={e=>setText(e.target.value)}rows={6}></textarea><button onClick={onGenerate}disabled={loading}>{loading?'Generating...':'Generate Quiz'}</button>{result&&<pre>{result}</pre>}</div>);}